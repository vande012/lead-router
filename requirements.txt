# Core dependencies for Lead Router automation
google-auth-oauthlib>=1.0.0,<2.0.0
google-auth-httplib2>=0.1.0,<1.0.0
google-api-python-client>=2.86.0,<3.0.0
selenium>=4.10.0,<5.0.0
python-dotenv>=1.0.0,<2.0.0

# Note: pandas was removed as it's not used in the code and causes issues with Python 3.13
# Note: webdriver-manager was removed as the script uses local chromedriver